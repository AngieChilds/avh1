 <%= form_for([@comp, @comp.prizes.build]) do |f| %>



  <div class="field">
    <%= f.label :text %>
    <%= f.text_field :text %>
  </div>
   
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
