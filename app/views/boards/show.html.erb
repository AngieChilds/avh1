
<p>
  <strong>Title:</strong>
  <%= @board.title %>
</p>
 
<p>
  <strong>Text:</strong>
  <%= @board.text %>
 </p>
 
 <h2>Comments</h2>
<%= render @board.comments %>
 
 <h2>Add a comment:</h2>
<%= render 'comments/form'  %>
  
  
 
<%= link_to 'Edit', edit_board_path(@board) %> |
<%= link_to 'Back', boards_path %>
